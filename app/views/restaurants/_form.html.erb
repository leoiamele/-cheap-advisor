<%= form_for(@restaurant) do |f| %>
 <%= f.label :name, "Enter name" %>
 <%= f.text_field :name %>

<%= f.label :address, "Enter an address" %>
<%= f.text_field :address %>

<%= f.label :rating, "Enter the rating" %>
<%= f.text_field :rating %>

<%= f.submit %>
		
<% end %>
